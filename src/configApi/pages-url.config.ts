class DASHBOARD{
    private root = '/i'
    HOME = this.root;
    TASKS = `${this.root}/tasks`;
    HABITS = `${this.root}/habits`;
    TIMER = `${this.root}/timer`;
    TIME_BLOCKing = `${this.root}/time-blocking`;
    SETTINGS = `${this.root}/settings`; 
}

export const PAGES_URL = new DASHBOARD();